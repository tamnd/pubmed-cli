---
title: "CLI"
description: "Every command and subcommand, with the flags that matter."
weight: 10
---

```
pubmed <command> [subcommand] [flags]
```

Run `pubmed <command> --help` for the full flag list on any command. This
page is the map; keep it in step with the real command tree as you add to it.

## Commands

| Command | What it does |
|---|---|
| `version` | Print the version and exit |

Add a row per command here as you build them out.
